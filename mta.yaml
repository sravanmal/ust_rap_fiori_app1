_schema-version: "3.2"
ID: travustsstravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travustsstravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travustsstravelproc-destination-service
    parameters:
      content-target: true
  - name: travustsstravelproc-repo-host
    parameters:
      service-key:
        name: travustsstravelproc-repo-host-key
  - name: travustsstravelproc-uaa
    parameters:
      service-key:
        name: travustsstravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travustsstravelproc_html_repo_host
          ServiceInstanceName: travustsstravelproc-html5-srv
          ServiceKeyName: travustsstravelproc-repo-host-key
          sap.cloud.service: travustsstravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travustsstravelproc_uaa
          ServiceInstanceName: travustsstravelproc-xsuaa-srv
          ServiceKeyName: travustsstravelproc-uaa-key
          sap.cloud.service: travustsstravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travustsstravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travustsstravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travustsstravelproc.zip
      name: travustsstravelproc
      target-path: resources/
- name: travustsstravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travustsstravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travustsstravelproc-destination-service
    service-plan: lite
- name: travustsstravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travustsstravelproc-xsuaa-srv
    service-plan: application
- name: travustsstravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travustsstravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
